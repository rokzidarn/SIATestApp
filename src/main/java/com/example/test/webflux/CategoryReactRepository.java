package com.example.test.webflux;

/*

import com.example.test.cassandra.Category;
import org.springframework.data.repository.reactive.ReactiveCrudRepository;

import java.util.UUID;

public interface CategoryReactRepository extends ReactiveCrudRepository<Category, UUID> {
    // ReactiveCassandraRepository
    // returns reactive data, as streams (Flux, Mono) directly, it calls subscribe automatically
}

*/